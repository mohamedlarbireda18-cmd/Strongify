
{
  "name": "fittrack-pro",
  "version": "1.0.0",
  "description": "FitTrack Pro - Application de suivi de musculation",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:front\" \"npm run dev:server\"",
    "dev:front": "cd front && npm run dev",
    "dev:server": "cd server && npm run dev",
    "build": "cd front && npm run build && cd ../server && npm run build",
    "install:all": "npm install && cd front && npm install && cd ../server && npm install",
    "db:generate": "cd server && npx prisma generate",
    "db:push": "cd server && npx prisma db push",
    "db:studio": "cd server && npx prisma studio"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
